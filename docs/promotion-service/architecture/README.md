# Promotion Service — Architecture

**Status:** Placeholder — empty until Phase 2 (Domain Model).

**Purpose:** Will hold the service-level architecture picture once the domain model exists — aggregate boundary diagram, dependency graph against other services (Order, Product, User, Payment), and how Promotion Service fits the platform's request/event flow. Mirrors the shape of [../../01-architecture-map.md](../../01-architecture-map.md), scoped to this service only.

Do not write content here before Phase 2 produces the aggregates it would describe.
