# Promotion Service — Enums

**Status:** Placeholder — empty until Phase 2 (Domain Model).

**Purpose:** Will hold the enum catalogue — one entry per enum, its values, and which entities reference it — kept current as each is introduced following step 4 of [../entities/entity-implementation-strategy.md](../entities/entity-implementation-strategy.md).

Do not write content here before Phase 2 produces the enums it would describe.
