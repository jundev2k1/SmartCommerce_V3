# Promotion Service — Tasks

**Status:** Pointer only, intentionally never populated with task files directly.

**Purpose:** This folder exists to satisfy the documented Phase 0 structure, but actual dated per-task tracking (bugs, gaps, follow-ups raised while implementing a phase) uses the repo-wide convention at [../../tasks/](../../tasks/) — see [../../tasks/README.md](../../tasks/README.md) — not a duplicate tracking system scoped to this service. Find Promotion Service tasks there, filtered by content (search for "Promotion").

If task volume for this service ever grows large enough to warrant its own index, add one here linking out to the relevant `../../tasks/<date>/TaskN_*.md` files — never move the task files themselves.
