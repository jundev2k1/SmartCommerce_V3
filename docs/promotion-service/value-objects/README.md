# Promotion Service — Value Objects

**Status:** Placeholder — empty until Phase 2 (Domain Model).

**Purpose:** Will hold one doc per PromotionService-local Value Object, written as each is introduced following step 5 of [../entities/entity-implementation-strategy.md](../entities/entity-implementation-strategy.md) — including which shared `BuildingBlock.Domain.ValueObjects` types were reused instead of duplicated.

Do not write content here before Phase 2 produces the Value Objects it would describe.
