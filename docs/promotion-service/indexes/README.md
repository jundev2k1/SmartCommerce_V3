# Promotion Service — Indexes

**Status:** Placeholder — empty until Phase 3 (Persistence).

**Purpose:** Will hold the index/unique-constraint catalogue — one entry per index, the entity/columns it covers, and the query it exists to serve — written alongside each `IEntityTypeConfiguration<T>` per [../phases/phase-3-persistence.md](../phases/phase-3-persistence.md).

Do not write content here before Phase 3 produces the indexes it would describe.
