# Task 9: Display TotalQuantity once it exists on the backend

**Status:** Blocked — depends on SimpleShop `docs/tasks/2026-07-27/Task4_order-missing-total-quantity.md`.

## Source

Full-system business-requirements audit, 2026-07-27.

## Current state

The backend has no `TotalQuantity` field at all today (see the blocking backend task), so there is nothing for the frontend to display yet.

## Suggested acceptance criteria (once unblocked)

- Surface `TotalQuantity` on `OrderSummaryCard.tsx` and `OrdersListPage.tsx`, alongside `TotalAmount`.

**Cross-ref:** SimpleShop `docs/tasks/2026-07-27/Task4_order-missing-total-quantity.md`.
